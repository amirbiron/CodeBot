# Base/shared runtime dependencies (no dev/test tools)

# Core Telegram Bot
python-telegram-bot[job-queue]==22.5

# Build tooling (needed for wheels during installs)
setuptools>=78.1.1,<81

# Database
pymongo==4.15.3
motor==3.7.1

# Code Processing & Language Detection
pygments==2.19.2
python-magic==0.4.27
chardet==5.2.0
langdetect==1.0.9
textstat==0.7.10

# Web Requests & APIs
requests==2.32.5
aiohttp==3.13.1
httpx==0.28.1
py_vapid==1.9.4  # 1.9.2 breaks with cryptography>=46 ("curve must be an EllipticCurve instance"); 1.9.3 has broken tar (absolute symlink)
pywebpush==2.2.0
# תלות משנית של pywebpush (המוצהר שם הוא http-ece>=1.1.0 — טווח פתוח).
# היא מבצעת את הצפנת ה-payload ונשענת על cryptography, ולכן שדרוג שקט שלה
# יכול לשבור שליחת פוש בלי שום שינוי בקוד. ננעל במפורש.
http-ece==1.2.1

# Google APIs (Drive OAuth + Client)
google-api-python-client==2.185.0
google-auth==2.41.1
google-auth-oauthlib==1.2.2

# GitHub Integration
PyGithub==2.8.1

# File Processing & Env
python-dotenv==1.2.1
# pathlib2 הוסר: לא נחוץ ב-Python 3

# Image Processing (for syntax highlighting images)
Pillow==12.0.0
cairosvg==2.8.2

# High-quality HTML→Image rendering (Chromium via Playwright)
playwright==1.49.0

# Utilities
python-dateutil==2.9.0.post0
pytz==2025.2
# מסד אזורי הזמן עבור zoneinfo. בלעדיו ZoneInfo("Asia/Jerusalem") נופל
# בסביבות שאין בהן tzdata מערכתי, ותצוגת הזמנים חוזרת ל-UTC בשקט.
tzdata==2026.3
click==8.3.0
arrow==1.4.0

# Logging & Monitoring
structlog==25.4.0
sentry-sdk[flask]==2.42.1
psutil==7.1.2
prometheus-client==0.23.1

# OpenTelemetry (tracing & basic auto-instrumentation)
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp
opentelemetry-exporter-prometheus
opentelemetry-instrumentation-flask
opentelemetry-instrumentation-requests
opentelemetry-instrumentation-pymongo

# Security
bcrypt==5.0.0
cryptography==46.0.3
bleach==6.3.0

# Web Server & HTTP layer
Flask==3.1.2
gunicorn==23.0.0
gevent==24.11.1
uvicorn==0.38.0
whitenoise==6.11.0
Flask-Compress==1.20

# MCP server (Model Context Protocol) — exposes user files to Claude via mcp_server/
# Compatible with the existing pins above (uvicorn>=0.31, pydantic>=2.11, httpx 0.28.x, starlette).
mcp==1.28.1

# PostHog MCP analytics — `posthog.mcp` ships inside the `posthog` package
# (no separate install). Requires >=7.21; pinned exactly like everything else
# here, and the MCP part of the SDK is pre-1.0, so a minor release may change
# the properties it emits. The allowlist in mcp_server/analytics.py is
# fail-closed against that, and tests/test_mcp_analytics_privacy.py fails
# loudly if a property this repo relies on is renamed.
posthog==7.45.3
# Installs from the published py3-none-any wheel, which is what every install
# path here uses — no --no-binary and no --no-build-isolation anywhere in the
# Dockerfile, the workflows or render.yaml. Worth knowing before that changes:
# the sdist declares `requires = ["setuptools>=83.0.0", "wheel"]`, above the
# `setuptools>=78.1.1,<81` pinned at the top of this file, so building posthog
# from source under that pin fails on the PEP 639 license field (reproduced).

# Rate limiting (Flask + generic)
Flask-Limiter==4.0.0
limits==5.6.0


# Async Support
aiofiles==25.1.0
asgiref==3.8.1

# Cache
redis==7.0.0

# Configuration
pyyaml==6.0.2
pydantic==2.12.3
pydantic-core==2.41.4
pydantic-settings==2.11.0

# HTML/Text Processing
beautifulsoup4==4.14.2
markdown==3.9
html2text==2025.4.15

# Version Control Integration
gitpython==3.1.45

# API Rate Limiting
ratelimit==2.2.1
slowapi==0.1.9

# Text Processing
rapidfuzz==3.14.1

# CLI Tools
# Flask-Limiter 3.5.0 דורש rich<14 — נצמיד לגרסה יציבה תואמת
rich==14.2.0
typer==0.20.0

# Resilience utilities
tenacity==9.1.2
